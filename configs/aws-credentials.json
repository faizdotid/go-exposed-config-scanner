{
    "id": "aws-credentials",
    "name": "AWS Credentials",
    "validator": "is_aws_credential",
    "match_from": "body",
    "output": "aws-credentials.txt",
    "paths": [".aws/credentials", ".aws/config", ".s3cfg"]
}
